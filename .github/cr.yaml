# The chart-releaser configuration file
# See: https://github.com/helm/chart-releaser#configuration

# The GitHub URL for creating releases
git-repo-url: https://github.com/antnsn/mimir-sync

# The GitHub Pages URL for the charts (gh-pages branch root)
github-pages-url: https://antnsn.github.io/mimir-sync

# The path to the directory containing Helm charts. The chart-releaser action
# input `charts_dir` overrides this anyway (we set it to "." in release.yaml),
# but keep this in sync for any local `cr` invocations.
charts-dir: .

# The branch used for GitHub Pages
github-pages-branch: gh-pages

# Skip existing packages (idempotent re-runs)
skip-existing: true

# Tag/release name template — use `v<version>` (e.g. v3.0.0) instead of the
# default `<chart>-<version>` so released tags match conventional semver tags
# referenced in our docs and external integrations.
release-name-template: "v{{ .Version }}"

# The commit message used when updating the index
commit-message: "chore(helm): update chart index [skip ci]"
