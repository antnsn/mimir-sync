{{- if and .Values.lokiRules.enabled (eq .Values.lokiRules.config.type "secret") (not .Values.lokiRules.config.existingName) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mimir-sync.lokiRulesConfigName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mimir-sync.labels" . | nindent 4 }}
    app.kubernetes.io/component: loki-rules-sync
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.lokiRules.rules }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
  {{- end }}
{{- end -}}
