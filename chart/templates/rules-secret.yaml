{{- if and .Values.rules.enabled (eq .Values.rules.config.type "secret") (not .Values.rules.config.existingName) -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mimir-sync.rulesConfigName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mimir-sync.labels" . | nindent 4 }}
    app.kubernetes.io/component: rules-sync
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.rules.rules }}
  {{ $key }}: |
    {{- $value | nindent 4 }}
  {{- end }}
{{- end -}}
